import React from 'react';
import AuthForm from '../components/AuthForm.connected';

function AuthContainer() {
  return (
    <div>
      <AuthForm />
    </div>
  );
}

export default AuthContainer;
